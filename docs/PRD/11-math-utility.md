# Issue 11 — Test TDD workflow: math utility

## Features

- Add a JavaScript math utility module (`src/math.js`) with an `add` function
- Write Vitest test cases in `tests/math.test.js`

## Acceptance Criteria

- [ ] `src/math.js` exports an `add(a, b)` function that returns the sum of two numbers
- [ ] `tests/math.test.js` contains at least 3 test cases covering: positive numbers, negative numbers, zero
- [ ] All tests pass with `npm test`
