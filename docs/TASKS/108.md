# Tasks: Add Audience / Spectators

## Parent Issue
#108

## Related Modules

- `src/court.js` — define spectator seating zones (positions around the court)
- `src/render.js` — draw audience figures each frame; manage idle/cheer animation states
- `src/main.js` — trigger crowd reactions on point-scored events (player or AI scores)
- `src/constants.js` — add audience-related constants (positions, density, animation timings)
- `tests/render.test.js` — add tests for audience rendering
- `tests/main.test.js` — add tests for audience reaction triggers

## Summary

Add ASCII spectators around the tennis court perimeter that visually react (cheer) when the player or opponent performs well (scores a point, wins a long rally). The audience is purely cosmetic and does not affect gameplay.

## Impact Analysis

| Module | Impact |
|--------|--------|
| `src/court.js` | Add `seating_zones` data structure defining spectator positions (rows behind baselines, along sidelines) |
| `src/render.js` | Add `render.audience()` function that draws each spectator figure using camera projection; add `cheer_level` animation state |
| `src/main.js` | Add `audience` state object; trigger `audience.cheer(winner)` when point resolves; pass audience state to render |
| `src/constants.js` | Add `SPECTATOR_COUNT`, `SEATING_ROWS`, `AUDIENCE_CHEER_DURATION`, spectator appearance colors |
| `tests/render.test.js` | Add tests: audience draws correct number of figures at correct positions; cheer state changes character; idle state renders correctly |
| `tests/main.test.js` | Add tests: audience cheer triggered on player point; audience cheer triggered on AI point; cheer timer expires and returns to idle |

## Phases

### Phase 1: Tests (TDD)
- [ ] `tests/main.test.js` — audience state is initialized with `idle` cheer level
- [ ] `tests/main.test.js` — `audience.cheer(winner)` sets cheer timer to non-zero for either winner
- [ ] `tests/main.test.js` — cheer timer decrements each frame and returns to idle at 0
- [ ] `tests/render.test.js` — `render.audience()` draws correct number of spectators in idle state
- [ ] `tests/render.test.js` — `render.audience()` draws excited spectators when cheer_level > 0

### Phase 2: Data Structures
- [ ] Add spectator positions to `src/court.js` (seating zones along sidelines and behind baselines)
- [ ] Add audience state management (`cheer_level`, `cheer_timer`) to `src/main.js` or new `src/audience.js`
- [ ] Add constants: `SPECTATOR_COUNT`, `AUDIENCE_CHEER_TIMER`, spectator color

### Phase 3: Core Logic
- [ ] Implement audience state: idle (cheer_level=0) → cheering (cheer_level>0) → decay
- [ ] Trigger `audience.cheer()` in `resolve_point()` and `resolve_violation_point()` on point resolution
- [ ] Pass audience state into render function each frame

### Phase 4: UI/Output
- [ ] Implement `render.audience(audience_state)` — iterate spectator positions, draw idle or cheering ASCII figures via camera projection
- [ ] Spectator idle pose: `O` (head) + `_` (body) or `o` + `:`
- [ ] Spectator cheer pose: `\o/` or `o/` (raised arms)
- [ ] Integrate into `draw_game()` after court/net but before players/ball
- [ ] Ensure proper z-ordering: audience behind the court is drawn behind court surface
